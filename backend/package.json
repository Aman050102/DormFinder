{
  "name": "dorm-finder-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply dorm-finder-db --local",
    "migrate:remote": "wrangler d1 migrations apply dorm-finder-db --remote",
    "db:create": "wrangler d1 create dorm-finder-db"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.6.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.5.4",
    "wrangler": "^3.70.0"
  }
}
